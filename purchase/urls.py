from django.urls import path

urlpatterns = [
    # TODO: path('/'), order 생성
    # TODO: path('<oid>/'), order 조회
    # TODO: path('<oid>/success/'), order 성공 (kp 전용)
    # TODO: path('<oid>/fail/'), order 실패 (kp 전용)
    # TODO: path('<oid>/cancel/'), order 취소 (kp 전용)
]
